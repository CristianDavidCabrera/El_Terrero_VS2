{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
 #}

{{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
    <section class="bg-gray-50 rounded-lg shadow dark:border md:mt-0 sm:max-w-lg xl:p-0 dark:bg-gray-700 dark:border-gray-700">
        {# <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0"> #}
            {# <div class="bg-gray-700 rounded-lg shadow dark:border md:mt-0 sm:max-w-lg xl:p-0 dark:bg-gray-700 dark:border-gray-700"> #}
                <div class="p-6 space-y-4 md:space-y-6 sm:p-8">                                                         
                    {{ form_widget(form, {
                        'attr': {'class': 'mx-auto bg-gray-50 border border-gray-300 text-gray-900 rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500' }
                    }) }}
                    <button class=" w-full bg-yellow-400 text-gray-800 hover:bg-yellow-500 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800">
                        {{ button_label|default('Save') }}  
                    </button>
                </div>
           {#  </div> #}
    {#  </div> #}
    </section>    
{{ form_end(form) }}


